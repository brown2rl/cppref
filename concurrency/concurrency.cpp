#include <iostream>
#include <string>
#include <vector>
#include <cmath>
#include <memory>
#include <thread>
#include <mutex>
#include <shared_mutex>
#include <future>
#include <condition_variable>

using namespace std;

int main()
{
 //concurrent loops, vectors, decks, queue, and cooperative scheduling for tasks are OS dependent.  

//concurrency is OS-dependent because system has managers
 return 0;
}
