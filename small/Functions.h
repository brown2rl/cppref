#include <string>
#include <vector>

using namespace std;

void fib(int start);
string manip(string a);
vector<string> fill_vec(int a, string b, vector<string> v); // a = how many items, b = item, v = a vector
void disp_vec(vector<string> v, char t);
int count_vec(vector<string> v, char c);
vector<string> sort_vec(vector<string> v);
int count_chars_vec(vector<string> v, char c);
