#include <vector>
#include <stack> //adapter

using namespace std;

int main()
{
// default container is a deque<_T>
// stack-like usage
  stack<int, vector<int>> a; // use vector storage
  s.push(1);
  
 return 0;
}
